import { Meta, StoryObj } from '@storybook/react'

const meta: Meta = {
  title: 'Pages/Auth',
}

export default meta
type Story = StoryObj<typeof meta>

export const Login: Story = {
  render: () => <>Test</>,
}
